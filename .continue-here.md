# Pause Work – Phase 06: Fix Apple‑Silicon SIGSEGV

## Summary
- Created new GSD phase directory `06-fix-apple-silicon-sigsegv`.
- Added:
  - `06-01-PLAN.md` – detailed plan to recreate a clean arm64 environment and verify fixes.
  - `RESEARCH.md` – audit steps for native extension binaries and OpenMP linkage.
  - `VERIFICATION.md` – checklist of conditions that must pass before closing the phase.

## Next Steps
1. Run the RESEARCH script (list packages, check otool output) and record any mismatches.
2. Follow the PLAN steps to rebuild the virtual environment and reinstall libraries.
3. Execute the import sanity test (faulthandler) and verify no SIGSEGV.
4. Run full training/quantization pipeline and ensure metrics improve.
5. Update VERIFICATION.md with pass/fail results and date.

## Resume Instructions
When ready, run `gsd-plan-phase` (or appropriate GSD command) in the `06-fix-apple-silicon-sigsegv` directory to continue work.
