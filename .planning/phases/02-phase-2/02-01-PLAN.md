---
phase: 02
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_info.py
autonomous: true
requirements:
  - REQ-TESTS-07
must_haves:
  truths:
    - "Test file exists and imports correctly"
    - "Unit tests can run without external dependencies"
    - "Tests cover registry query, output formatting, and error handling"
  artifacts:
    - path: tests/test_info.py
      provides: "Unit tests for info command module"
      min_lines: 100
  key_links:
    - from: "tests/test_info.py"
      to: "mmcli/info.py"
      via: "imports and mock subprocess calls"
      pattern: "from mmcli.info import|import mmcli\.info"
---

## Unit Tests for Info Command

This plan creates comprehensive unit tests for the `mmcli info` command module. The tests verify registry query behavior, output formatting, and error handling without requiring an actual tinyml_modelmaker installation.

**Purpose:** Ensure the info command works correctly across different scenarios including missing dependencies, invalid task types, and edge cases in JSON parsing.

**Output:** `tests/test_info.py` with pytest-style test classes and functions.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-tdd.md
</execution_context>

<context>
@.planning/phases/02-phase-2/RESEARCH.md

## Key Types and Interfaces

From mmcli/info.py:
```python
def _run_query(python_exe: str, script: str) -> dict:
    """Run *script* via *python_exe* and return parsed JSON."""

def run_info(args, python_exe: str) -> None:
    """Run the info subcommand."""
```

## Test Strategy
1. Mock subprocess.run to return predefined JSON responses
2. Test _run_query with valid and invalid outputs
3. Test run_info for different argument combinations
4. Test error handling paths (missing registry, JSON parse errors)
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Create test_info.py with test scaffolding</name>
  <files>tests/test_info.py</files>
  <behavior>
    - Test 1: _run_query returns parsed JSON dict for valid output
    - Test 2: _run_query raises SystemExit for invalid JSON
    - Test 3: _run_query raises SystemExit for non-zero exit code
    - Test 4: run_info displays devices list correctly
    - Test 5: run_info displays models list correctly
    - Test 6: run_info handles error responses from registry
    - Test 7: _group_devices groups devices by family correctly
    - Test 8: _bin_dataset returns correct bucket for sample counts
  </behavior>
  <action>Create tests/test_info.py with pytest-style unit tests for mmcli/info.py module. Use unittest.mock to patch subprocess.run and sys.exit. Cover: registry query (valid/invalid output), device grouping, dataset binning, error handling paths.</action>
  <verify>
    <automated>pytest tests/test_info.py -v --tb=short</automated>
  </verify>
  <done>Test file created with 8+ test functions; pytest runs successfully with all tests passing (mocked)</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess → output parsing | External process output must be validated before JSON parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | I | _run_query JSON parsing | mitigate | Test with malformed JSON to ensure error handling works correctly |

## Security Considerations

The info command uses `shell=False` and argument arrays. Tests will verify:
- Malformed JSON responses are handled gracefully
- Subprocess errors produce appropriate error output
</threat_model>

<verification>
- pytest tests/test_info.py passes with all mocked tests
- No external dependencies required for test execution
- Test coverage includes success and failure paths in info.py
</verification>

<success_criteria>
- File tests/test_info.py exists with 100+ lines of test code
- 8+ test functions covering all public functions in info.py
- All tests pass when run with `pytest tests/test_info.py -v`
</success_criteria>

<output>
Create `.planning/phases/02-phase-2/02-01-SUMMARY.md` when done
</output>
