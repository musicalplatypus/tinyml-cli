---
phase: 02
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_analyze.py
autonomous: true
requirements:
  - REQ-TESTS-07
must_haves:
  truths:
    - "Test file exists and imports correctly"
    - "Tests cover dataset analysis (classes/files layout)"
    - "Tests handle edge cases in data files"
  artifacts:
    - path: tests/test_analyze.py
      provides: "Unit tests for analyze command module"
      min_lines: 100
  key_links:
    - from: "tests/test_analyze.py"
      to: "mmcli/analyze.py"
      via: "imports and mock filesystem operations"
      pattern: "from mmcli.analyze import|import mmcli\.analyze"
---

## Unit Tests for Analyze Command

This plan creates comprehensive unit tests for the `mmcli analyze` command module. The tests verify dataset analysis functionality for both classes/ and files/ layouts, including edge cases in data file handling.

**Purpose:** Ensure the analyze command correctly reads various data formats (.csv, .npy, .pkl), calculates sample counts, and determines dataset size buckets.

**Output:** `tests/test_analyze.py` with pytest-style test classes and functions.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-tdd.md
</execution_context>

<context>
@.planning/phases/02-phase-2/RESEARCH.md

## Key Types and Interfaces

From mmcli/analyze.py:
```python
def _bin_dataset(n: int) -> str:
    """Return dataset size bucket for sample count."""

def _row_count(file_path: str) -> int:
    """Return number of samples in a data file (first dimension)."""

def analyse_dataset(dataset_path: str) -> dict[str, Any]:
    """Auto-detect layout and return analysis dict."""
```

## Test Strategy
1. Use temp directory fixtures to create test dataset structures
2. Create mock CSV/NPY/PKL files with known sample counts
3. Test _bin_dataset for boundary values (499, 500, 4999, 5000)
4. Test _row_count for each file type (.csv, .npy, .pkl)
5. Test analyse_dataset for both classes/ and files/ layouts
6. Test error handling for missing directories
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Create test_analyze.py with test scaffolding</name>
  <files>tests/test_analyze.py</files>
  <behavior>
    - Test 1: _bin_dataset returns 'tiny' for n < 500
    - Test 2: _bin_dataset returns 'small' for 500 <= n < 5000
    - Test 3: _bin_dataset returns 'medium' for 5000 <= n < 50000
    - Test 4: _bin_dataset returns 'large' for n >= 50000
    - Test 5: _row_count correctly counts CSV rows
    - Test 6: _row_count correctly counts NPY array first dimension
    - Test 7: _row_count handles pickle files
    - Test 8: analyse_dataset detects classes/ layout
    - Test 9: analyse_dataset detects files/ layout
    - Test 10: analyse_dataset returns error for missing directories
  </behavior>
  <action>Create tests/test_analyze.py with pytest-style unit tests for mmcli/analyze.py module. Use tempfile and pathlib to create test data structures. Mock numpy/pandas imports where needed to avoid external dependencies.</action>
  <verify>
    <automated>pytest tests/test_analyze.py -v --tb=short</automated>
  </verify>
  <done>Test file created with 10+ test functions; pytest runs successfully with all tests passing (mocked)</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → data parsing | Invalid/malformed files should not crash the analyzer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | I | File format validation | mitigate | Test with malformed CSV/NPY/PKL files to ensure error handling works correctly |

## Security Considerations

The analyze command handles user-provided paths. Tests will verify:
- Path traversal protection (already in cli.py validation)
- Invalid file formats don't crash the analyzer
</threat_model>

<verification>
- pytest tests/test_analyze.py passes with all mocked tests
- No external dependencies required for test execution
- Test coverage includes success and failure paths in analyze.py
</verification>

<success_criteria>
- File tests/test_analyze.py exists with 100+ lines of test code
- 10+ test functions covering all public functions in analyze.py
- All tests pass when run with `pytest tests/test_analyze.py -v`
</success_criteria>

<output>
Create `.planning/phases/02-phase-2/02-02-SUMMARY.md` when done
</output>
