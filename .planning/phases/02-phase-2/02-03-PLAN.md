---
phase: 02
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_recommend.py
autonomous: true
requirements:
  - REQ-TESTS-07
must_haves:
  truths:
    - "Test file exists and imports correctly"
    - "Tests cover model scoring algorithm"
    - "Tests handle missing modelzoo path gracefully"
  artifacts:
    - path: tests/test_recommend.py
      provides: "Unit tests for recommend command module"
      min_lines: 100
  key_links:
    - from: "tests/test_recommend.py"
      to: "mmcli/recommend.py"
      via: "imports and mock filesystem operations"
      pattern: "from mmcli.recommend import|import mmcli\.recommend"
---

## Unit Tests for Recommend Command

This plan creates comprehensive unit tests for the `mmcli recommend` command module. The tests verify the model scoring algorithm, example file parsing, and recommendation display logic.

**Purpose:** Ensure the recommend command correctly parses config files, scores examples against user criteria, and displays recommendations in a human-readable format.

**Output:** `tests/test_recommend.py` with pytest-style test classes and functions.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-tdd.md
</execution_context>

<context>
@.planning/phases/02-phase-2/RESEARCH.md

## Key Types and Interfaces

From mmcli/recommend.py:
```python
def _parse_model_params(model_name: str) -> Optional[int]:
    """Parse parameter count from model name (e.g., 'CLS_1k_NPU' -> 1000)."""

def get_recommendations(...) -> dict[str, Any]:
    """Get ranked recommendations based on scoring."""

def print_recommendations(result: dict, top_n: int = 5) -> None:
    """Display formatted recommendations."""
```

## Test Strategy
1. Create temp directory with mock config files
2. Test _parse_model_params for various naming patterns
3. Test _score_example for matching/non-matching criteria
4. Test get_recommendations with valid and missing modelzoo paths
5. Test print_recommendations output formatting
6. Test task type to module mapping
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Create test_recommend.py with test scaffolding</name>
  <files>tests/test_recommend.py</files>
  <behavior>
    - Test 1: _parse_model_params extracts 'k' suffix correctly (e.g., 'CLS_1k_NPU')
    - Test 2: _parse_model_params handles multiple numbers in name
    - Test 3: _score_example returns correct score for matching criteria
    - Test 4: _score_example adds +2 bonus for variables match
    - Test 5: _score_example considers dataset size restriction
    - Test 6: get_recommendations returns error when modelzoo not found
    - Test 7: TASK_TYPE_TO_MODULE mapping is correct for all task types
    - Test 8: print_recommendations displays formatted output without errors
    - Test 9: Dataset preferred max params lookup works correctly
    - Test 10: Complexity tier classification works correctly
  </behavior>
  <action>Create tests/test_recommend.py with pytest-style unit tests for mmcli/recommend.py module. Use tempfile to create mock config files. Test scoring logic thoroughly with various input combinations.</action>
  <verify>
    <automated>pytest tests/test_recommend.py -v --tb=short</automated>
  </verify>
  <done>Test file created with 10+ test functions; pytest runs successfully with all tests passing (mocked)</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config parsing → data extraction | Malformed YAML files should not crash the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | I | YAML parsing validation | mitigate | Test with malformed config.yaml files to ensure error handling works correctly |

## Security Considerations

The recommend command reads user-provided paths. Tests will verify:
- Path traversal protection (already in cli.py validation)
- Invalid config files don't crash the parser
</threat_model>

<verification>
- pytest tests/test_recommend.py passes with all mocked tests
- No external dependencies required for test execution
- Test coverage includes success and failure paths in recommend.py
</verification>

<success_criteria>
- File tests/test_recommend.py exists with 100+ lines of test code
- 10+ test functions covering all public functions in recommend.py
- All tests pass when run with `pytest tests/test_recommend.py -v`
</success_criteria>

<output>
Create `.planning/phases/02-phase-2/02-03-SUMMARY.md` when done
</output>
