---
wave: 2
depends_on: ["07-01"]
type: execute
files_modified: []
autonomous: true
---

# Plan 07-02: Regression Guard — Full Test Suite

## Objective

Confirm that the Phase 7 CLI additions (from plan 07-01) cause zero regressions in the
existing test suite.

## Background

Plan 07-01 modified `mmcli/cli.py` (TARGET_DEVICES, TASK_TYPES_AUDIO, two help strings)
and created `tests/test_device_task_coverage.py`. This plan verifies nothing else broke.

## Tasks

### Task 1: Run full test suite and verify no regressions

<read_first>
- tests/ (all test files — understand what was passing before Phase 7)
</read_first>

<action>
Run: `pytest tests/ -v --tb=short`

If any pre-existing test fails:
1. Identify which test and what error
2. Check whether the failure is caused by Phase 7 changes in mmcli/cli.py
3. Fix the regression in mmcli/cli.py without removing or weakening the failing test
4. Re-run until `pytest tests/ -v` exits 0

Do NOT delete or weaken any existing tests to resolve failures.
</action>

<acceptance_criteria>
- `pytest tests/ -q --tb=short` exits 0
- No pre-existing test newly fails after Phase 7 changes
- `tests/test_device_task_coverage.py` (from 07-01) is also included and passes
- Total test count equals or exceeds count before Phase 7 (no tests deleted)
</acceptance_criteria>

---

## must_haves

- `pytest tests/ -q` exits 0 (no regressions)

## Verification

```bash
pytest tests/ -q --tb=short
```
