---
wave: 1
depends_on: []
type: tdd
files_modified:
  - tests/test_device_task_coverage.py
  - mmcli/cli.py
autonomous: true
---

# Plan 07-01: TDD — Device & Task Coverage (F28E12 + audio_classification)

## Objective

Implement CLI device and task type coverage completeness through a RED → GREEN TDD cycle.

**Purpose:** Tests drive implementation — write failing assertions first, then add the constants
that make them pass. Confirms both F28E12 and TASK_TYPES_AUDIO are correctly integrated before
treating the work as done.

**Output:** Working, tested CLI coverage with 2 atomic commits (RED + GREEN).

## Background

See `07-RESEARCH.md`. Key facts:
- `choices=TARGET_DEVICES` causes hard rejection at `mmcli info -d F28E12` (lines 907, 993, 1024).
- `TASK_TYPES_AUDIO` constant does not exist — `audio_classification` is invisible to users.
- `mmcli/info.py` already imports the audio module (line 34) — no info.py changes needed.
- NAS_SUPPORTED_TASKS must NOT be modified (audio_classification is not a NAS task).

## Feature

```
<feature>
  <name>CLI device and task type coverage completeness</name>
  <files>tests/test_device_task_coverage.py, mmcli/cli.py</files>
  <behavior>
    "F28E12" in TARGET_DEVICES → True
    TASK_TYPES_AUDIO importable from mmcli.cli → True
    "audio_classification" in TASK_TYPES_AUDIO → True
    TASK_TYPES_TIMESERIES, TASK_TYPES_VISION, TASK_TYPES_AUDIO are pairwise disjoint
    mmcli train --help includes "audio_classification"
    mmcli train --help includes "F28E12"
  </behavior>
  <implementation>
    Add "F28E12" between "F2837" and "F28P55" in TARGET_DEVICES (cli.py:100).
    Add TASK_TYPES_AUDIO = ["audio_classification"] after TASK_TYPES_VISION (cli.py:96-97).
    Append "\nAudio tasks:\n  audio_classification" to --task help in _add_common_args.
    Add F28E12 to C2000 line in --device help text (cli.py ~line 277).
  </implementation>
</feature>
```

## Tasks

### Task 1 (RED): Write failing tests

<read_first>
- mmcli/cli.py (lines 84–120, TARGET_DEVICES and TASK_TYPES constants)
- tests/ (existing test file naming conventions and import patterns)
</read_first>

<action>
Create `tests/test_device_task_coverage.py` importing TARGET_DEVICES, TASK_TYPES_TIMESERIES,
TASK_TYPES_VISION, and TASK_TYPES_AUDIO from `mmcli.cli`. Include these 12 tests:

Class `TestDeviceCoverage` (4 tests):
- `test_f28e12_in_target_devices`: `assert "F28E12" in TARGET_DEVICES`
- `test_f28e12_accepted_by_argparse`: `subprocess mmcli train -d F28E12 --help` exits 0 and "F28E12" in stdout
- `test_no_duplicates_in_target_devices`: `assert len(TARGET_DEVICES) == len(set(TARGET_DEVICES))`
- `test_all_c2000_devices_present`: `c2000_expected = {"F280013","F280015","F28003","F28004","F2837","F28E12","F28P55","F28P65","F29H85","F29P58","F29P32"}; assert c2000_expected.issubset(set(TARGET_DEVICES))`

Class `TestAudioTaskDiscoverability` (5 tests):
- `test_task_types_audio_exists`: `assert isinstance(TASK_TYPES_AUDIO, list) and len(TASK_TYPES_AUDIO) > 0`
- `test_audio_classification_in_task_types_audio`: `assert "audio_classification" in TASK_TYPES_AUDIO`
- `test_audio_classification_in_train_help`: subprocess `mmcli train --help`, assert "audio_classification" in stdout
- `test_audio_classification_in_run_help`: subprocess `mmcli run --help`, assert "audio_classification" in stdout
- `test_audio_module_accepted`: subprocess `mmcli info -m audio --help`, assert exits 0

Class `TestTaskTypeSetsExclusive` (3 tests):
- `test_timeseries_vision_disjoint`: `assert not set(TASK_TYPES_TIMESERIES) & set(TASK_TYPES_VISION)`
- `test_timeseries_audio_disjoint`: `assert not set(TASK_TYPES_TIMESERIES) & set(TASK_TYPES_AUDIO)`
- `test_vision_audio_disjoint`: `assert not set(TASK_TYPES_VISION) & set(TASK_TYPES_AUDIO)`

After writing, run: `pytest tests/test_device_task_coverage.py -v`
Expected result: tests FAIL — `ImportError` on `TASK_TYPES_AUDIO` OR assertion errors on F28E12.
This is the RED gate — failure confirms tests are testing something real.

Commit (RED gate):
`test(07-01): add failing tests for F28E12 and TASK_TYPES_AUDIO coverage`
</action>

<acceptance_criteria>
- `tests/test_device_task_coverage.py` exists and is syntactically valid
- `pytest tests/test_device_task_coverage.py -v` exits non-zero (RED — expected failures)
- Failure reason is ImportError on TASK_TYPES_AUDIO or assertion error on F28E12
- `git log --oneline | head -1` shows a commit with prefix `test(07-01):`
</acceptance_criteria>

---

### Task 2 (GREEN): Implement to make tests pass

<read_first>
- mmcli/cli.py (lines 84–120, TARGET_DEVICES and TASK_TYPES block)
- mmcli/cli.py (lines 250–290, _add_common_args --task and --device help strings)
- tests/test_device_task_coverage.py (tests from Task 1 — understand what must pass)
</read_first>

<action>
In `mmcli/cli.py` make exactly four edits:

1. `TARGET_DEVICES` C2000 block (line 100): change
   `"F280013", "F280015", "F28003", "F28004", "F2837",`
   to
   `"F280013", "F280015", "F28003", "F28004", "F2837", "F28E12",`

2. After `TASK_TYPES_VISION = ["image_classification"]` (line 96), add:
   `TASK_TYPES_AUDIO = ["audio_classification"]`

3. In `_add_common_args`, find the `--task` argument's `help=` string. Locate the line that ends
   with `"Vision tasks:\n  image_classification"` and append `"\nAudio tasks:\n  audio_classification"`

4. In `_add_common_args`, find the `--device` argument help string. The C2000 line reads
   `"C2000:     F280013  F280015  F28003  F28004  F2837\n"`. Add F28E12 so it reads
   `"C2000:     F280013  F280015  F28003  F28004  F2837  F28E12\n"`

After changes run: `pytest tests/test_device_task_coverage.py -v`
Expected result: ALL tests PASS (GREEN gate).

Commit (GREEN gate):
`feat(07-01): add F28E12 device and TASK_TYPES_AUDIO constant to CLI`
</action>

<acceptance_criteria>
- `grep "F28E12" mmcli/cli.py | grep TARGET` exits 0
- `grep "TASK_TYPES_AUDIO" mmcli/cli.py` exits 0
- `python -m mmcli train --help | grep "audio_classification"` exits 0
- `python -m mmcli train --help | grep "F28E12"` exits 0
- `pytest tests/test_device_task_coverage.py -v` exits 0 — all 12 tests pass (GREEN)
- `git log --oneline | head -1` shows a commit with prefix `feat(07-01):`
</acceptance_criteria>

---

## must_haves

- RED commit (`test(07-01):...`) exists in git log BEFORE GREEN commit (`feat(07-01):...`)
- `F28E12` is in `TARGET_DEVICES` — accepted by info/deploy subcommands (choices= gate)
- `TASK_TYPES_AUDIO` is importable from `mmcli.cli`
- No existing device or task entries removed or renamed
- `NAS_SUPPORTED_TASKS` is NOT modified
- `mmcli/info.py` is NOT modified

## Verification

```bash
git log --oneline | head -3   # verify RED then GREEN commit order
pytest tests/test_device_task_coverage.py -v
python -m mmcli train --help | grep -E "F28E12|audio_classification"
```
