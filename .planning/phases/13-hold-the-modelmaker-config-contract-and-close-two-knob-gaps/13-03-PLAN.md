---
phase: 13-hold-the-modelmaker-config-contract-and-close-two-knob-gaps
plan: 03
type: execute
wave: 2
gap_closure: true
depends_on: [13-01, 13-02]
files_modified:
  - tests/test_workflow.py
  - tests/test_tier4_cli.py
autonomous: true
requirements: [REQ-CUDA-01]
---

<objective>
Ten tests fail on `main`, and none of them is a production defect. They were passing by asserting
`rc == 0` on configs that would have failed later at training time; channel-aware preset selection
(`a7804ca`) now catches both problems at config-generation time, so the tests fail where they used
to pass silently.

**Do not "fix" this by weakening the selector.** Its behaviour is correct in both cases. Fix the
tests so they assert what is actually true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mmcli/preset_selection.py
@tests/test_workflow.py
@tests/test_tier4_cli.py

<diagnosis_already_established>
Verified — do not re-derive:

**Cause A — `tests/test_workflow.py::test_dry_run_train` (1 failure).** Its fixture writes
`feature,label\n1,0\n...` — two columns, one being a label. Real classification data does not look
like that. `~/Documents/edgeai/myproject1/dataset/classes/sawtooth/saw10.csv`, from the project that
trained successfully on 2026-08-14, is a **single unlabelled column with no header**; the class
comes from the `classes/<class>/` directory, which is also why the fixture separately writes
`annotations/labels.csv`. The fixture contradicts itself, and `detect_channels` correctly counts 2.

`generic_timeseries_classification` has 19 presets: 17 expect 1 channel, 2 expect 3, **none accepts
2**. So the error is accurate.

**Cause B — `tests/test_tier4_cli.py::TestDryRunCrossDevice` forecasting cases (3+ failures).**
`generic_timeseries_forecasting` has **0 presets at all**. This is finding F-2 from the
training-matrix sweep: an upstream catalog gap. `choose_preset` already reports it exactly right —
*"Task 'x' has no feature-extraction presets available... This is a gap in the upstream preset
catalog, not a problem with your dataset."*
</diagnosis_already_established>
</context>

<tasks>

<task type="auto">
  <name>Task 1: make the classification fixture look like real data</name>
  <files>tests/test_workflow.py</files>
  <action>
Change the fixture CSV to match how real classification datasets are actually shaped: a single
unlabelled data column, class conveyed by the `classes/<class>/` directory. Keep the
`annotations/labels.csv` the fixture already writes — that part is right.

Leave `assert rc == 0` in place. With a realistic 1-channel fixture the selector finds one of the
17 matching presets and the dry-run should succeed, which is the whole point: **the test should
pass because the input is now valid, not because the check was relaxed.**

Add a comment saying why the shape matters, citing the real dataset path — otherwise someone will
"tidy" a header back in later.

If it still fails after the fixture is corrected, **stop and report**. That would mean the
diagnosis is wrong and something in production needs looking at, which is a different plan.
  </action>
  <verify>
    <automated>MMCLI_PYTHON=/Users/martin/.venv-tinyml/bin/python /Users/martin/.venv-tinyml/bin/python -m pytest tests/test_workflow.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>The fixture is single-column and unlabelled, the test passes for the right reason, and a comment records why the shape matters.</done>
</task>

<task type="auto">
  <name>Task 2: forecasting must assert the truth, not a success it cannot have</name>
  <files>tests/test_tier4_cli.py</files>
  <action>
The forecasting cases assert a dry-run succeeds for a task with **zero** feature-extraction presets.
That cannot be true, and asserting it hid the gap.

**Be careful here — this is the "don't encode broken behaviour as expected" trap, with a twist.**
What is broken is *upstream*, not mmcli: mmcli's behaviour (a clear, specific error naming the
catalog gap) is correct and should be pinned as such. So assert that mmcli **fails cleanly with the
upstream-gap message**, rather than either (a) asserting success, or (b) deleting the case.

Requirements for how you do it:
- The assertion must be specific enough to distinguish the upstream-gap error from any other
  failure. A bare "non-zero exit" would pass for a crash, a typo, or an unrelated regression.
- Reference **F-2** and `.planning/FINDINGS-training-matrix.md` in a comment, so that when upstream
  adds forecasting presets someone knows to revisit this rather than puzzling over why a test
  expects failure.
- Do **not** apply this to other task types in the same parametrisation that genuinely do have
  presets — only forecasting is affected. Check which parametrised cases actually fail rather than
  assuming the set; the full-suite run showed 10 failures and only some are forecasting.

State in the SUMMARY how many cases you changed and which task types they covered.
  </action>
  <verify>
    <automated>MMCLI_PYTHON=/Users/martin/.venv-tinyml/bin/python /Users/martin/.venv-tinyml/bin/python -m pytest tests/test_tier4_cli.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>Forecasting cases pin the specific upstream-gap error; unaffected task types are untouched; F-2 is cited so the expectation is revisited when upstream changes.</done>
</task>

</tasks>

<verification>
- `tests/test_workflow.py` and `tests/test_tier4_cli.py` both green.
- **No change to `mmcli/`.** This plan is test-only; the selector's behaviour is correct in both
  cases. A production change here would be a finding to report, not a fix to make.
- Do not run the full suite — it takes ~14 minutes. The orchestrator will run it at merge.
</verification>
