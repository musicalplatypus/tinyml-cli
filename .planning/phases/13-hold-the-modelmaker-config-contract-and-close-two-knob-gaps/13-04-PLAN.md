---
phase: 13-hold-the-modelmaker-config-contract-and-close-two-knob-gaps
plan: 04
type: execute
wave: 3
gap_closure: true
depends_on: [13-03]
files_modified:
  - tests/test_cli_integration.py
  - tests/test_tier4_cli.py
  - .planning/FINDINGS-training-matrix.md
autonomous: true
requirements: [REQ-CUDA-01]
---

<objective>
Finish what 13-03 started. Six failures remain, all the same root cause seen from three angles, and
one of them exists because **13-03's plan scoped the wrong file set** — `tests/test_cli_integration.py`
was never in scope. That is a planning error, not an execution one.

Also record a finding that has no home yet: `generic_timeseries_regression` has no usable preset for
any realistic dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@mmcli/preset_selection.py
@.planning/FINDINGS-training-matrix.md
@tests/test_cli_integration.py
@tests/test_tier4_cli.py

<measured_state>
Queried through `mmcli.preset_selection.query_presets` on 2026-08-15:

| task | presets | usable | usable channel widths |
|---|---|---|---|
| `generic_timeseries_classification` | 19 | 17 | **[1, 3]** — works |
| `generic_timeseries_regression` | 2 | 1 | **[11]** |
| `generic_timeseries_forecasting` | 0 | 0 | — |
| `generic_timeseries_anomalydetection` | 0 | 0 | — |

**Two different failure paths in `choose_preset`, and they produce different messages:**
- **Zero presets** (`forecasting`, `anomalydetection`) → the `if not presets` branch:
  *"Task '…' has no feature-extraction presets available … a gap in the upstream preset catalog."*
- **Presets exist but none matches the channel count** (`regression`) → the `if not usable` branch:
  *"No usable feature-extraction preset for task '…' matches the N input channel(s)…"*

The six remaining failures: `regression` ×4 (1 in `test_cli_integration`, 3 in `test_tier4_cli`),
`forecasting` ×1 and `anomalydetection` ×1 (both in `test_cli_integration`).
</measured_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pin the right error for each task type, in the file 13-03 missed</name>
  <files>tests/test_cli_integration.py, tests/test_tier4_cli.py</files>
  <action>
Apply 13-03's approach to the remaining cases, following the pattern it established in
`test_tier4_cli.py` rather than inventing a second style — read it first.

**The two paths produce different messages and must be asserted separately.** Pinning the
zero-presets wording on `regression` (or vice versa) would pass today and stop being meaningful the
moment upstream changes one and not the other. Assert the message that path actually emits.

Cases to cover:
- `test_cli_integration.py`: `generic_timeseries_regression`, `generic_timeseries_forecasting`,
  `generic_timeseries_anomalydetection`
- `test_tier4_cli.py`: `generic_timeseries_regression` × 3 devices

**Leave `classification` alone everywhere** — it has 17 usable presets and passes.

Cite F-2 for the zero-preset tasks and the new F-9 (Task 2) for regression, so a future reader can
tell which upstream gap each expectation is waiting on.
  </action>
  <verify>
    <automated>MMCLI_PYTHON=/Users/martin/.venv-tinyml/bin/python /Users/martin/.venv-tinyml/bin/python -m pytest tests/test_cli_integration.py tests/test_tier4_cli.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>All six cases pin the message their own failure path emits; classification untouched; each expectation cites the finding it depends on.</done>
</task>

<task type="auto">
  <name>Task 2: record the regression gap as F-9</name>
  <files>.planning/FINDINGS-training-matrix.md</files>
  <action>
`generic_timeseries_regression` is not tracked. Add it as **F-9**, in the file's existing style —
read the surrounding entries and match them rather than imposing a new format.

The substance: 2 presets, 1 usable, and that one requires **11 input channels**. The other
(`Custom_Default`) matches a realistic 1-channel shape but declares no `feat_ext_transform`, so it
produces no features. A realistic regression dataset therefore has no usable preset — structurally
the same as F-5 but for a different task type, and distinct from F-2 (which is *zero* presets).

Note the wider picture while you are there, because it is more useful than the individual entries:
**three of the four generic timeseries task types cannot auto-select a preset** — only
classification can. Whatever form the file uses for cross-cutting observations, put it there.

Do not overstate it: this is an upstream catalog gap, not an mmcli defect. mmcli reports it
correctly and refuses to guess, which is the intended behaviour.
  </action>
  <verify>
    <automated>grep -c "F-9" .planning/FINDINGS-training-matrix.md</automated>
  </verify>
  <done>F-9 is recorded in the file's own style, distinguished from F-2 and F-5, with the three-of-four observation captured and no overstatement of mmcli's role.</done>
</task>

</tasks>

<verification>
- `tests/test_cli_integration.py` and `tests/test_tier4_cli.py` green.
- **No change to `mmcli/`.** The selector is correct; this is test and documentation work.
- Do not run the full suite — it takes ~14.5 minutes. The orchestrator runs it at merge.
</verification>
