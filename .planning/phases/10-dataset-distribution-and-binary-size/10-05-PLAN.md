---
wave: 5
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - README.md
  - README_zh.md
  - docs/RELEASING.md
  - docs/mmcli.rst
autonomous: true
---

# Plan 10-05: Documentation

## Objective

Update user-facing documentation for on-demand datasets, and write the release-process
document the versioning scheme depends on.

Runs last because it documents the interfaces settled by 10-02 (`datasets` subcommand),
10-03 (unbundling and asset publication) and 10-04 (the app's download affordance).

## Background

Three distinct gaps, only one of which is "missing docs".

**1. Existing documentation becomes factually wrong.** `README.md:736` states:

```
| `MMCLI_DATASETS` | bundled `example_datasets/` | Override directory containing example dataset zips |
```

After D-2 nothing is bundled, so both the stated default and the word "Override" are wrong.
`README.md:507` shows `export MMCLI_DATASETS=...` without explaining that it now also
disables fetching. Wrong documentation is worse than none — this is the same failure as the
`build_macos.sh` "~10 MB" comment that masked the bloat for so long.

**2. `README_zh.md` mentions `MMCLI_DATASETS` twice** and will silently drift out of sync
with the English README.

**3. There is no release-process document at all**, and D-3 places obligations on whoever
cuts a release: decide whether to bump `DATASETS_DEFAULT_VERSION` to a newer TI engine
version, re-record digests if TI republished a file, and confirm the pinned URLs still
resolve. If those obligations live only in a plan file, the scheme rots the first time
someone else cuts a release — and the failure mode is silent (a stale pin serving old data),
not a crash.

Sphinx already autodocs `mmcli.datasets` (`docs/mmcli.rst:39-42`), so new public functions
appear automatically provided their docstrings are written.

## Tasks

### Task 1: Correct and extend README.md

<read_first>
- README.md (line ~507 `export MMCLI_DATASETS`, line ~736 env var table)
</read_first>

<action>
Fix the env var table row — the default is no longer a bundled directory, and setting the
variable disables fetching entirely rather than merely overriding a path.

Add a Datasets section covering:
- the nine TI datasets are fetched from software-dl.ti.com on first use; the 18 KB audio set ships in the binary
- `mmcli datasets list / pull <name> / path <name>`
- the cache at `~/.cache/mmcli/datasets/<version>/`, and that it is keyed by TI engine version
- **the offline recipe, explicitly**: download the ten assets once, put them in a directory,
  export `MMCLI_DATASETS`. This is the supported path for air-gapped and corporate networks
  and must not survive as folklore.

State that a first `init --dataset` needs network unless `MMCLI_DATASETS` is set. That is a
real behaviour regression from the bundled build and users should meet it in the docs rather
than in a failure.
</action>

<verify>
<automated>
grep -q "datasets pull" README.md && grep -q "cache/mmcli/datasets" README.md && ! grep -q "bundled \`example_datasets/\`" README.md && echo OK
</automated>
</verify>

### Task 2: Keep the Chinese README honest

<read_first>
- README_zh.md (both `MMCLI_DATASETS` occurrences)
</read_first>

<action>
Update the same two facts if you can do so accurately. If not, add a short note at the top of
`README_zh.md` stating which section is out of date and pointing at `README.md` as
authoritative.

Do not machine-translate the new Datasets section and present it as reviewed text — a
confidently wrong translation of the offline recipe is worse than an honest pointer.
</action>

<verify>
Manual: a zh reader is not left with a statement that contradicts the English README.
</verify>

### Task 3: Write docs/RELEASING.md

<read_first>
- mmcli/datasets.py (`DATASETS_DEFAULT_VERSION`, per-entry `ti_name` / `ti_version` / `sha256`)
- .planning/phases/10-dataset-distribution-and-binary-size/10-RESEARCH.md (D-1/D-2/D-3)
</read_first>

<action>
Create `docs/RELEASING.md` covering the dataset obligations of a release:

1. **Cutting a release** — decide whether `DATASETS_DEFAULT_VERSION` should move to a newer
   TI engine version (e.g. `01_03_00` -> `01_04_00`). Datasets that should stay on the old
   version get a per-entry `ti_version` override.
2. **Digests are the contract.** If TI republishes a file, its sha256 changes and every
   client fails verification loudly. That is intended — it must never install unexpected
   data — but it means bumping the version requires re-recording digests, and the error a
   user sees should be traceable back to this step.
3. **Adding a new dataset** — add the registry entry with `filename`, `ti_name`, `sha256`
   (`shasum -a 256`), and confirm `mmcli datasets pull <name>` works from a clean cache.
4. **Verification before announcing** — from a machine that has never built the repo:
   `rm -rf ~/.cache/mmcli/datasets && mmcli datasets pull <each>`. TI reorganising paths is
   the most likely breakage and this is what catches it.
5. **If TI ever breaks the URLs** — mirroring the zips to this repo's release assets remains
   a viable fallback; the registry change is a URL swap, not a redesign.

Explain *why* for each rule. A rule whose reason is unstated gets "optimised away" by the
next person; the immutability rule in particular looks like pointless bureaucracy until you
know clients cache by digest.
</action>

<verify>
<automated>
test -f docs/RELEASING.md && grep -q "DATASETS_DEFAULT_VERSION" docs/RELEASING.md && grep -qi "digest" docs/RELEASING.md && echo OK
</automated>
</verify>

### Task 4: CLI help and API docs

<read_first>
- mmcli/cli.py (the `datasets` subparser added in 10-02; the env var help block)
- docs/mmcli.rst (autodoc entry for `mmcli.datasets`)
</read_first>

<action>
Ensure `mmcli datasets --help` and each of `list`/`pull`/`path` carry a usage example, in the
style of the existing subcommands.

Update the env-var help text in `cli.py` to match the corrected README wording — the two
drifting apart is how the "~10 MB" claim survived.

Confirm the new public functions in `datasets.py` (`dataset_url`, `fetch_dataset`,
`_cache_dir`) have docstrings good enough for the existing Sphinx autodoc, and that
`docs/` still builds without warnings.
</action>

<verify>
<automated>
./dist/mmcli datasets --help | grep -q "pull" && cd docs && sphinx-build -W -b html . _build/html 2>&1 | tail -1
</automated>
`-W` turns warnings into errors, matching the "builds cleanly with no warnings" bar set in
Phase 3.
</verify>

## Success Criteria

- No statement in `README.md` about dataset location or `MMCLI_DATASETS` is false after
  unbundling
- The offline/air-gapped recipe is written down and reproducible from the README alone
- `docs/RELEASING.md` tells a future maintainer exactly what to do with datasets when cutting
  a release, and why
- `mmcli datasets --help` is useful without reading source
- Sphinx builds clean with `-W`
- `README_zh.md` does not contradict the English README

## Notes

Documentation is a deliverable of this phase, not a postscript. The root cause of the bloat
was a comment asserting an invariant nothing enforced — `build_macos.sh:4` claimed "~10 MB"
for months while shipping 260 MB. Leaving the same class of stale claim in the README after
this change would repeat that mistake.
