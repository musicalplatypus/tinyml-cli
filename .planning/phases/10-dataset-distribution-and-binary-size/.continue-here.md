# Continue here — 10-09 is mid-flight at its checkpoint

**Written:** 2026-08-02
**Applies to:** `10-09-PLAN.md` (PlatypusStudio dataset library + `mmcli datasets remove`)
**State:** Tasks 1-3 complete and committed **in two repos**. Task 4 — a blocking
human-verification checkpoint — has not been attempted. No SUMMARY written yet; it is written
after the checkpoint clears.

Nothing here is a blocking anti-pattern. This file exists because five commits across two
repositories are otherwise invisible to anyone resuming, and `ROADMAP.md` still shows 10-09
unchecked (correctly — the plan is not finished).

## What is already committed

| Task | Repo | Commit | What |
|---|---|---|---|
| 1 | tinyml-cli | `47c76f3` | `mmcli datasets remove`, `cache_entry_path()`, legible ENOSPC, additive `cache_bytes` in `datasets list --format json` |
| 1 (doc) | tinyml-cli | `420adde` | `10-06-SUMMARY.md` JSON-contract section updated for `cache_bytes` (D-10) |
| 2 | PlatypusStudio | `6d66d7f` | `DatasetCatalog` extended: removal, `cacheBytes`, transfer-failure classifier; new `DatasetLibraryTests.swift` |
| 2 (dev.) | PlatypusStudio | `560aa63` | `DatasetInfo.sizeBytes` — deviation, see below |
| 3 | PlatypusStudio | `e5142bf` | `DatasetLibraryView.swift` + Manage Datasets toolbar item in `WorkspaceView.swift` |

Both working trees were clean at handoff. tinyml-cli: 99 tests green (35 in
`test_datasets_cli.py`). PlatypusStudio: **163 pass, 1 skipped** (baseline was 140+1).

The managed mmcli copy at `~/Library/Application Support/PlatypusStudio/bin/mmcli` **was
refreshed** (25,256,704 bytes) so the GUI sees `datasets remove` and `cache_bytes`. If that
copy is ever replaced with an older build, the library will silently read a stale contract.

## What remains

**Task 4 only** — drive the real app. CONTEXT `D-09` records that the user chose to have Claude
drive the UI rather than perform the checks themselves; the same constraint applies as for
10-04's checkpoint: report what was *observed*, name what could not be exercised, and do not
claim a clean pass on inference.

The nine checks, and the state each needs, are listed in the executor's handoff — reproduced in
short form:

1. `rm -rf ~/.cache/mmcli/datasets`, `MMCLI_DATASETS` unset → open **Manage Datasets** from the
   workspace toolbar with no project open; 10 datasets listed with sizes
2. Affordances match the approved table; `generic_audio_classification` offers no Download
3. Download a small one (`generic_timeseries_forecasting`, 71 KB) → row flips to Downloaded +
   Remove, footer count and size increase
4. Remove it → row returns to Download, footer decreases, file gone from the cache dir
5. **hard gate** — Cancel a large download mid-transfer (`fan_blade_fault`, 56.6 MB): stops, no
   surviving child process, no file left in the cache
6. Network off → Download gives an offline message carrying mmcli's own text
7. Integrity path — only the wording is unit-covered; a genuine checksum mismatch was not driven
8. `MMCLI_DATASETS` → empty dir, relaunch: everything `unavailable`, no Download anywhere
9. **hard gate** — `MMCLI_DATASETS` holding a real zip, then `mmcli datasets remove <name>` in a
   terminal: exit 0, informational NOTE, **the file in that directory still present**

## Deviations already taken (must appear in the eventual SUMMARY)

1. `DatasetInfo.sizeBytes` added — the model only carried a byte count for `.downloadable`,
   which could not satisfy this plan's own must-have that every dataset shows its size.
2. Swapped `///` doc comments on `.downloadable` / `.cached` in `DatasetAvailability` corrected
   (from 10-04). Documentation only; all call sites were already correct.
3. The plan's verification names a literal `grep -n -- '--fetch'` expected to return nothing on
   `DatasetCatalog.swift`, but that file already contained `--fetch` inside a doc comment
   written in 10-04. The executor left the accurate wording alone rather than reword docs to
   satisfy a literal grep. The real constraint — `--fetch` is never passed as a subprocess
   argument — holds and was verified by inspection. **The SUMMARY should say this plainly, and
   the grep in the plan is the thing that is wrong, not the comment.**

## Context decisions folded in beyond the plan text

`10-CONTEXT.md` postdates this plan. D-10 (per-dataset reclaimable cache size, additive JSON
field), D-11 (total cache size + location), D-12 (bulk download), D-13 (task/module filter) were
all implemented as part of Tasks 1 and 3 and must be credited in the SUMMARY as
context-driven scope, not invented scope.
