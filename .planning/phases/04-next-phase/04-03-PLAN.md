# Plan 04-03: Security Documentation

**Phase:** 04  
**Plan Number:** 04-03  
**Type:** doc  
**Priority:** Medium  
**Date:** 2026-07-06  

## Objective

Create comprehensive security documentation including threat model, secure coding guidelines, and incident response procedures.

## Background

**Context:** Phase 1 established security patterns but there's no consolidated security documentation. Users and contributors need guidance on mmcli's security posture.

## Implementation Plan

### Task 1: Create SECURITY.md

<action>Create `SECURITY.md` at project root with security contact info and policy.</action>

```markdown
# Security Policy

## Reporting a Vulnerability

If you discover a security vulnerability in mmcli, please report it responsibly:

1. **DO NOT** open a public GitHub issue
2. Email the maintainers at security@mmcli.example.com
3. Include details about the vulnerability and reproduction steps

We aim to respond within 48 hours and fix critical issues within 7 days.

## Supported Versions

| Version | Supported          |
|---------|--------------------|
| 1.x     | :white_check_mark: |
| < 1.0   | :x:                |

## Security Features

- Input validation on all user-provided strings
- Subprocess execution with shell=False
- Path traversal protection
- Environment variable sanitization
- Timeout enforcement on subprocess calls

## Known Limitations

- No built-in encryption for sensitive data
- Dependencies must be manually audited
- No automated security scanning in CI (planned)
```

<verify>File exists: `test -f SECURITY.md && echo "SECURITY.md created"`</verify>

<done>SECURITY.md is created with contact info and policy.</done>

### Task 2: Update README with security section

<action>Add security considerations to main README.md.</action>

```markdown
## Security Considerations

### Input Validation
All user inputs are sanitized before use:
- Command arguments are stripped of shell metacharacters
- File paths are validated to prevent directory traversal
- Environment variables are cleaned before subprocess calls

### Subprocess Security
All subprocess calls use `shell=False` with argument arrays to prevent command injection.

### Path Validation
Project paths must be relative or within safe directories (not absolute paths starting with `/`).
```

<verify>README has security section: `grep -A5 "Security Considerations" README.md`</verify>

<done>README updated with security considerations.</done>

### Task 3: Document environment variable security

<action>Create docs/ENVIRONMENT_SECURITY.md explaining MMCLI_* vars security.</action>

```markdown
# Environment Variable Security

## mmcli Environment Variables

mmcli uses the following environment variables:

| Variable | Purpose | Security Notes |
|----------|---------|----------------|
| `MMCLI_PYTHON` | Path to Python executable | Must be valid path, sanitized before use |
| `MMCLI_MODELZOO_PATH` | Path to model zoo directory | Path traversal blocked |
| `MMCLI_MODELMAKER` | Path to tinyml_modelmaker | Input validation applied |

## Security Best Practices

1. **Never** set environment variables from untrusted sources
2. **Validate** any MMCLI_* values before processing
3. **Use absolute paths** when possible (easier to validate)
4. **Avoid** shell metacharacters in paths

## Environment Variable Injection Prevention

mmcli sanitizes all environment variable values:
```python
def _sanitize_env_var(value: str) -> str:
    return re.sub(r'[^\w\-./_ ]', '', value)
```
```

<verify>Documentation exists: `test -f docs/ENVIRONMENT_SECURITY.md`</verify>

<done>Environment security documentation is created.</done>

### Task 4: Add security patterns to codebase

<action>Add security documentation as docstrings in key modules.</action>

```python
# mmcli/cli.py (add module-level docstring)
"""
mmcli - TI TinyML ModelMaker CLI

Security Considerations:
- All user inputs are sanitized before use
- Subprocess calls use shell=False with argument arrays
- Path traversal is prevented via _is_safe_path()
- Timeouts prevent hanging subprocesses
"""

# mmcli/info.py (add docstring to functions)
def _run_query(python_exe: str, script: str) -> dict:
    """
    Query the tinyml_modelmaker registry.
    
    Security: Uses shell=False and argument arrays to prevent
    command injection via python_exe or script parameters.
    """
```

<verify>Docstrings exist: `grep -r "Security Considerations" mmcli/`</verify>

<done>All key modules have security docstrings.</done>

## Verification

```bash
# Verify all documentation files exist
test -f SECURITY.md && echo "SECURITY.md OK"
test -f docs/ENVIRONMENT_SECURITY.md && echo "Environment Security OK"

# Check README has security section
grep -q "Security Considerations" README.md && echo "README OK"

# Run documentation build (if sphinx is configured)
sphinx-build -b html docs/ docs/_build/html 2>&1 | head -20
```

## Acceptance Criteria

- [ ] SECURITY.md created at project root
- [ ] README updated with security section
- [ ] Environment variable security documented
- [ ] Key functions have security docstrings
- [ ] Documentation builds without errors (if using Sphinx)
